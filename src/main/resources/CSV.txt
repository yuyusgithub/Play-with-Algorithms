csv
将csv作为表来处理
数据以文本方式存储在文件中；
.CSV文件存贮表内容
.CSM文件存贮标的元数据，如状态和数据量
.frm服务器层的存贮表结构信息

特点
将csv作为表来处理
所有的列都是不能为NULL的 建表的时候，得添加not null约束
不支持索引，不适合大表，不适合在线处理
可以对文本文件直接进行编辑

合适作为数据交换的中间表，如excel 导出csv文件，再加到csv表中，就能在mysql中索引了
